When the assistant's visible emotional tone should change, call `set_visual_emotion`.
Choose only from the mapped emotions exposed by `get_available_visual_options`.
Use `neutral` when the extra emotional coloring should clear and the base activity state should stand on its own again.
Do not call the emotion tool on every turn if the visible emotion should stay the same.
